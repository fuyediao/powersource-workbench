# Required for Google auth + cloud library (renderer + main).
# Derives https://supabase.{domain} and https://api.{domain}.
VITE_DEPLOYMENT_DOMAIN=your-domain.example
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional
# VITE_ALLOWED_AUTH_EMAILS=you@example.com
