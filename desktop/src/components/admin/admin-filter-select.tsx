/**
 * Admin alias for the shared CRM filter select (backward-compatible imports).
 */

export {
  CrmFilterSelect as AdminFilterSelect,
  type CrmFilterOption as AdminFilterOption,
} from '@/components/common/crm-filter-select'
