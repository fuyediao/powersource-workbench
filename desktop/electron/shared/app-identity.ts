/** Dock, menu, and window display name (not the npm package id). */
export const APP_DISPLAY_NAME = 'PowerSource Workbench'

/** Internal product id used in schemes, IPC, storage keys, and package names. */
export const APP_INTERNAL_NAME = 'Workbench'
